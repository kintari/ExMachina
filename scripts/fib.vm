
function fib(x : uint) : uint {
    if (x == 0) {
        return 0;
    }
    else if (x == 1) {
        return 1;
    }
    else {
        return fib(x-2) + fib(x-1);
    }
}

println("fib(5) is ", fib(5));